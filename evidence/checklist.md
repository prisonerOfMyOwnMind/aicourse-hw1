# Чек-лист ручной проверки на устройстве

Устройство: Xiaomi Redmi Note 10S (M2101K7BNY), Android 13, API 33, 1080x2400, density 440.

Заполняется по ходу проверки. Пустая отметка означает, что проверка не проводилась —
непроведённая проверка не считается пройденной.

| # | Сценарий | Ожидаемое | Факт | Артефакт |
|---|---|---|---|---|
| 1 | Установка на устройство | `./gradlew installDebug` завершается успехом | Провалено на этом телефоне: MIUI отдаёт `INSTALL_FAILED_USER_RESTRICTED`. Обход найден — `adb install -r -t` того же APK возвращает `Success` | `install-fail.log` |
| 2 | Первый запуск | Меню: три уровня, рекордов нет, «Продолжить» отсутствует | Сначала не совпало: кнопка «Продолжить» была, на диске лежал файл партии `NOT_STARTED`. После удаления файла и перезапуска состояние верное. Появление файла без действий воспроизвести не удалось | `01-menu-empty.png` |
| 3 | Начало партии | Все клетки закрыты, таймер 00:00, счётчик равен числу мин | Пройдено | `02a-menu-saved-game.png` (меню), партия видна на `04-game-playing.png` |
| 4 | Первый тап | Открывается область больше одной клетки, мины под тапом нет | Пройдено: раскрылась область в 49 клеток, подрыва не было | `04-game-playing.png`, `saved-game-after-kill.txt` |
| 5 | Долгий тап | Ставится флажок, счётчик мин уменьшается | Пройдено: счётчик 10 → 9, в файле `flagged=43`, и клетка 43 действительно в списке мин | `04-game-playing.png` |
| 6 | Короткий тап по флажку | Ничего не происходит, клетка не открывается | Не проверялось руками. Покрыто unit-тестом `короткий тап по клетке с флажком ничего не делает` | `gradle-test.log` |
| 7 | Перерасход флажков | Счётчик уходит в минус, вёрстка не ломается | Не проверялось на устройстве. Логика покрыта тестом `счётчик мин уходит в минус при перерасходе флажков` | `gradle-test.log` |
| 8 | Поражение | Мины раскрыты, подорванная выделена, неверные флажки перечёркнуты, оверлей с кнопками | Не проверялось на устройстве | |
| 9 | Победа | Оставшиеся мины помечены флажками, счётчик 0, оверлей с временем | Не проверялось на устройстве | |
| 10 | Рекорд | После победы в меню появляется лучшее время уровня | Не проверялось на устройстве | |
| 11 | Убийство процесса | Ход, флажок, сворачивание, убийство, запуск — партия, флажки и время на месте | **Пройдено.** До: ⚑9, таймер 00:25. Процесс убит смахиванием из свитчера. После «Продолжить»: поле идентично, флажок на месте, таймер 00:29 при сохранённых на диске 27,8 с | `proof-kill-process.mp4`, `04-game-playing.png` → `02a-menu-saved-game.png` → `05-restored-after-kill.png` |
| 12 | «Don't keep activities» | При включённой опции партия не теряется | Не проверялось: опция включается в настройках телефона, `settings put` заблокирован MIUI | |
| 13 | Таймер в фоне | Прибавка за время в фоне близка к нулю | **Пройдено.** В фоне телефон провёл около 80 секунд, таймер прибавил 16 — ровно время видимого экрана после возврата. Шёл бы календарно, показал бы около 01:48 вместо 00:44 | `05-restored-after-kill.png`, `06-restored-timer-continues.png` |
| 14 | Поворот экрана | Во всех восьми состояниях вёрстка цела, партия не потеряна, прокрутки нет | Не проверялось: поворот требует касаний или `settings put`, заблокированного MIUI | |
| 15 | Тёмная тема | Все состояния читаемы | **Дефект найден и исправлен.** Сначала темнели только клетки, а фон вокруг поля оставался белым: XML-тема окна была жёстко светлой, а контент не был обёрнут в `Surface`. После правки фон тёмный целиком | до: `07-game-dark-before-fix.png`, после: `08-menu-dark.png` |
| 16 | Системный шрифт 200 % | Таймер, счётчик, кнопки, рекорды не обрезаны и не наезжают | Не проверялось: `settings put system font_scale` заблокирован MIUI (`WRITE_SETTINGS`) | |
| 17 | Узкий экран | `adb shell wm size 720x1600` — поле помещается без прокрутки | Не проверялось: `wm size` заблокирован MIUI (`WRITE_SECURE_SETTINGS`) | |
| 18 | Авиарежим | Полная партия до победы при включённом авиарежиме | Не проверялось прогоном. Косвенно: разрешения `INTERNET` нет ни в исходнике, ни в собранном APK, то есть сетевых вызовов быть не может | `static-checks.log` |
| 19 | Испорченный файл партии | Приложение открывается без «Продолжить», рекорды на месте, падения нет | **Пройдено.** Файл заменён на `мусор-без-ключей`. После запуска: «Продолжить» отсутствует, рекорд `Новичок 00:42` на месте, нечитаемый файл удалён приложением, 0 строк `FATAL EXCEPTION` | `corrupt-files.log`, `09-menu-records-survived-corrupt-game.png` |
| 20 | Испорченный файл рекордов | Блок рекордов пуст, падения нет | **Пройдено.** Файл заменён на `сломано`. После запуска блок рекордов пуст, файл удалён приложением, 0 строк `FATAL EXCEPTION` | `corrupt-files.log`, `10-menu-after-corrupt-records.png` |
| 21 | Системная «Назад» | На экране партии уводит в меню, партия сохранена | Не подтверждено артефактом: в кадры перехода не попало | |
| 22 | Подтверждение потери партии | Выбор уровня при живой партии показывает диалог | Не подтверждено артефактом: в кадры не попало | |

**Отдельно про рекорд в пункте 19.** Файл рекордов записан вручную через `run-as`,
а не выигран в игре. Проверялся разбор файла и независимость чтения партии и рекордов,
а не игровое достижение. Реальная победа на устройстве не проверялась.

## Проверки без устройства

Выполнены командами, вывод целиком в `static-checks.log` и `gradle-test.log`.

| Проверка | Результат | Артефакт |
|---|---|---|
| 56 unit-тестов ядра на JVM | Пройдены, 0 провалов. Число тестов и провалов снято из XML-отчётов, а не из строки «BUILD SUCCESSFUL» | `gradle-test.log` |
| Разрешение `INTERNET` в манифесте | Не объявлено. Первая версия проверки дала ложное «нарушение», поймав слово в собственном комментарии — переписана на поиск объявления | `static-checks.log` |
| Разрешения в собранном APK | Проверены через `aapt2 dump permissions`: только автоматическое `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` от AndroidX, сетевых нет | `static-checks.log` |
| Ядро игры не зависит от Android | Ни одного `import android` в пакете `game` | `static-checks.log` |
| Внешние зависимости | Только Compose, Material 3, activity-compose, lifecycle-viewmodel-compose и JUnit для тестов | `static-checks.log` |
